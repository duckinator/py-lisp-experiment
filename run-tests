#!/bin/sh

# This file is just so I can hit F5 in nvim instead of switching back
# and forth to a terminal, for a quicker feedback cycle.
#
# You don't need to care about it. :)

#python3.10 -m lisp
python3.10 -m pytest --verbose
